# Protect API directory
Options -Indexes

# Deny access to config and includes
<FilesMatch "^(includes|db-setup\.sql|reminder_log\.txt|\.htaccess)">
    Require all denied
</FilesMatch>

# Block direct access to includes directory
RewriteEngine On
RewriteRule ^includes/ - [F,L]
